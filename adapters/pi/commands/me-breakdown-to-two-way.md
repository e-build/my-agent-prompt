---
description: Break down an unknown codebase area into a structured two-way analysis (learning + design) markdown output
argument-hint: "<topic>"
---

Read `resources/commands/me-breakdown-to-two-way.md` and follow its instructions exactly.
If `resources/commands/` is not in the working directory, read it from `~/.pi/agent/commands/`.

Arguments: $@
