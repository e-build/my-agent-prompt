---
description: Break down an unknown codebase area into a structured two-way analysis (learning + design) markdown output
argument-hint: "<topic>"
---

Read `bodies/me-breakdown-to-two-way.md` and follow its instructions exactly.
If `bodies/` is not in the working directory, read it from `~/.pi/agent/bodies/`.

The Documentation Specialist role rules are in `bodies/doc-manager.md`
(resolve from the same location as the body file above).

Arguments: $@
